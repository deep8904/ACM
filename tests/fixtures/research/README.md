# Offline research fixtures

This set covers official, release-note, technical, community, specification, conflict, malformed, missing-date, plain-text, JSON, XML, and robots cases. Redirect, private-address, oversized, unsupported, cancellation, duplicate-content, manual-topic, and manual-URL cases use synthetic `Response` and repository fixtures in tests so no network request can occur.
